fund=false

# Keep npm state inside the repo: the DSH agent sandbox cannot write to ~/.npm,
# which surfaces as a misleading "cache folder contains root-owned files" EPERM.
# A relative cache path keeps `npm run pack:check` (and any install) working
# both in that sandbox and on a normal machine.
cache=.npm-cache
